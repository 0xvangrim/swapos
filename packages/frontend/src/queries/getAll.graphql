query GetAllHTLCs @live {
  htlcerc20S(orderBy: createdAt, orderDirection: desc) {
    id
    sender
    senderAmount
    senderDomain
    senderToken
    receiverDomain
    receiverAmount
    receiverToken
    sendStatus
  }
}
